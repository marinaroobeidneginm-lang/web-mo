{
  "version": 2,
  "installCommand": "npm install --prefix client --include=dev && npm install --prefix server",
  "buildCommand": "npm run build --prefix client && npm run db:generate --prefix server",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "server/app.js": {
      "maxDuration": 30,
      "includeFiles": "server/node_modules/.prisma/**"
    }
  }
}
